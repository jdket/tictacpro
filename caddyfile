:{$PORT}

root * /app/public
file_server

# Allow embedding in iframes
header {
  -X-Frame-Options
  Content-Security-Policy "frame-ancestors *"
}

# SPA fallback (if your game uses JS routing)
@notFile {
  not file
}
rewrite @notFile /index.html
